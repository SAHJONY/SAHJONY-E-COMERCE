import Link from "next/link";
import { ORDER_WORKFLOW, products, evaluate } from "@/lib/commerce";

export default function Operations(){const approved=products.filter(p=>evaluate(p).status==="approved").length;return <main className="shell"><nav><Link href="/" className="logo">SAHJONY</Link><div className="links"><Link href="/">STORE</Link></div></nav><section className="model"><div className="eyebrow">OWNER OPERATIONS OS</div><h1>BUY-AFTER-SALE<br/>CONTROL.</h1><div className="grid"><article className="card"><small>DEMO SKUS</small><h3>{products.length}</h3></article><article className="card"><small>PROCUREMENT READY</small><h3>{approved}</h3></article><article className="card"><small>MARGIN GATE</small><h3>25%+</h3></article></div></section><section className="shop"><header><div><div className="eyebrow">ORDER WORKFLOW</div><h2>Customer → SAHJONY → Customer</h2></div></header><div className="steps">{ORDER_WORKFLOW.map((s,i)=><span key={s}>{String(i+1).padStart(2,"0")} {s.replaceAll("_"," ")}</span>)}</div><p style={{marginTop:28,color:"#6d6962"}}>Production is fail-closed: no live payment capture or supplier purchase is executed until payment, database, tax, sourcing, returns and carrier integrations are verified.</p></section></main>}
